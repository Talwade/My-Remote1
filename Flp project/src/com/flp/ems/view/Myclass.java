package com.flp.ems.view;

public class Myclass {

}

/*
 * HashMap<String, Integer> names=new LinkedHashMap<>();
 * ArrayList<String> array=new ArrayList(); while(scan.hasNext()) {
 * 
 * 
 * }
 * 
 * System.out.println("Enter the noof employee you want");
 * countofemp=scan.nextInt(); for(int i=0;i<countofemp;i++) {
 * 
 * System.out.println("Enter employee id"); empid=scan.nextInt();
 * System.out.println("Enter employee name"); name=scan.next();
 * System.out.println("Enter kinid"); kinid=scan.nextInt();
 * 
 * System.out.println("Enter employee mailid"); mailid=scan.next();
 * 
 * System.out.println("Enter employee phone number");
 * phone=scan.nextInt(); System.out.println("Enter employee name");
 * name=scan.next();
 * 
 * 
 * 
 * 
 * 
 * 
 * }
 */
/*Map<Integer, String> mapStudent = new HashMap<>();*/

/*	System.out.println("Enter the role id of employee");
	roleid = scan.nextInt();
	System.out.println("Enter the project id");
	projectid = scan.nextInt();*/
	// Random generation of kin

	
/*	number = Phone_no;*/
	//validate();
	//isValidEmailAddress(Email_id);


	// add the key value pair from user input to the hashmap

/*	Integer oldid = mapStudent.put("Empid", idNumber);
	String names = mapStudent.put("Empname", name);*/

	/*
	 * if (oldVal!=null) { System.out.println("Student Id Number:" +
	 * idNumber + " is " + oldVal + " and has been overwritten by " +
	 * name); }
	 */
	// ask user to check if another entry is required
	
/*// ask for user input for id number
System.out.print("Enter ID number:");
Integer idNumber = Integer.parseInt(scan.nextLine());*/

// ask for user input which corresponds to name